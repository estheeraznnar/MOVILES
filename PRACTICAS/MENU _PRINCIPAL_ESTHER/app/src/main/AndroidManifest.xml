<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._03_Menu_Principal">
        <activity
            android:name=".registro_superheroes.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".registro_superheroes.DetalleHeroeActivity"
            android:exported="false" />
        <activity
            android:name=".quizz.QuizzResultadoActivity"
            android:exported="false" />
        <activity
            android:name=".quizz.QuizzPrincipalActivity"
            android:exported="false" />
        <activity
            android:name=".calculadora.CalculadoraActivity"
            android:exported="false" />
        <activity
            android:name=".login.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".edad_canina.EdadcaninaActivity"
            android:exported="false" />
        <activity
            android:name=".apirazas.RazasApiActivity"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 6 - Le damos permisos a nuestro provider-->
        <!-- Un provider es un componente que permite compartir datos entre aplicaciones de manera segura -->
        <provider
            android:authorities="${applicationId}.provider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>